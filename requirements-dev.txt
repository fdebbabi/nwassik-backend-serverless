# Development and testing dependencies
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
python-dotenv>=1.0.0

# AWS SDK (Lambda has built-in, but needed for local dev/tests)
boto3>=1.40.0

# Mocking AWS services (for future unit/integration tests)
moto>=5.0.0
