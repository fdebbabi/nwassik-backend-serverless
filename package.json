{
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  },
  "scripts": {
    "info": "dotenv -- serverless info --stage",
    "deploy": "dotenv -- serverless deploy --stage",
    "remove": "dotenv -- serverless remove --stage",
    "init-db": "dotenv -- venv/bin/python scripts/init_db.py"
  }
}